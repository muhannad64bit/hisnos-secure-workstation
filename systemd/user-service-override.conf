# systemd/user-service-override.conf
# Installs to /etc/systemd/system/service.d/99-hisnos-isolation.conf

[Unit]
# Ensure no weak dependencies block the boot graph
After=graphical.target

[Service]
Type=idle
CPUWeight=10
Nice=10
MemoryMax=300M
TasksMax=200
StartLimitBurst=3
